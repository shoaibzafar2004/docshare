# Path to the SQLite database file.
# Locally this defaults to ./data/app.db if unset.
# On Railway, mount a volume (e.g. at /data) and set this to /data/app.db
# so documents survive restarts and redeploys.
DATABASE_PATH=./data/app.db
